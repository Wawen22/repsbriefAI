# RepsBrief Launch Readiness Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Portare RepsBrief a uno stato di lancio professionale e pronto a fatturare: brand coerente con logo reale, SEO corretto, copy onesto sul pricing, e social proof reale.

**Architecture:** 4 task indipendenti e in sequenza: (1) SEO metadata root layout, (2) logo reale in tutta l'app, (3) fix copy pricing misleading, (4) social proof dinamica con dato reale da Supabase via API route.

**Tech Stack:** Next.js App Router, TypeScript, Tailwind, Supabase (admin client), `next/image` per logo

---

## Task 1: SEO Metadata — Fix root layout (CRITICO per lancio)

Il layout root ha ancora `title: "Create Next App"` e `description: "Generated by create next app"`. Questo è il primo testo che Google indicizza e che viene mostrato nelle anteprime social.

**Files:**
- Modify: `src/app/layout.tsx`

- [ ] **Step 1: Sostituisci il blocco `metadata` in `src/app/layout.tsx`**

Apri `src/app/layout.tsx` e sostituisci l'intero oggetto `metadata`:

```typescript
export const metadata: Metadata = {
  metadataBase: new URL('https://repsbrief.com'),
  title: {
    default: 'RepsBrief — AI Content Briefs from Real Trends',
    template: '%s | RepsBrief',
  },
  description:
    'Generate high-impact weekly content briefs powered by real Reddit, YouTube, and Google trends. Built for solo creators and content studios.',
  keywords: ['content brief', 'content strategy', 'AI content', 'trend analysis', 'content creator tools'],
  authors: [{ name: 'RepsBrief' }],
  creator: 'RepsBrief',
  openGraph: {
    type: 'website',
    locale: 'en_US',
    url: 'https://repsbrief.com',
    title: 'RepsBrief — AI Content Briefs from Real Trends',
    description:
      'Generate high-impact weekly content briefs powered by real Reddit, YouTube, and Google trends.',
    siteName: 'RepsBrief',
    images: [
      {
        url: '/logo.png',
        width: 512,
        height: 512,
        alt: 'RepsBrief Logo',
      },
    ],
  },
  twitter: {
    card: 'summary',
    title: 'RepsBrief — AI Content Briefs from Real Trends',
    description:
      'Generate high-impact weekly content briefs powered by real Reddit, YouTube, and Google trends.',
    images: ['/logo.png'],
  },
  robots: {
    index: true,
    follow: true,
    googleBot: {
      index: true,
      follow: true,
      'max-video-preview': -1,
      'max-image-preview': 'large',
      'max-snippet': -1,
    },
  },
}
```

- [ ] **Step 2: Commit**

```bash
git add src/app/layout.tsx
git commit -m "feat: add proper SEO metadata, OG tags, and Twitter card"
```

---

## Task 2: Logo reale in tutta l'app

Sostituisci l'icona `<Zap>` con `<Image src="/logo.png">` in 5 file. La dimensione display è 32×32 in sidebar/navbar, 40×40 nelle auth pages.

**Files:**
- Modify: `src/components/landing/Navbar.tsx`
- Modify: `src/components/layout/DashboardSidebar.tsx`
- Modify: `src/components/layout/MobileNav.tsx`
- Modify: `src/app/(auth)/login/page.tsx`
- Modify: `src/app/(auth)/signup/page.tsx`

- [ ] **Step 1: Landing Navbar — `src/components/landing/Navbar.tsx`**

Aggiungi l'import di `Image` in cima al file (dopo gli import esistenti):
```typescript
import Image from 'next/image'
```

Rimuovi `Zap` dall'import di `lucide-react` (non serve più nel logo).

Sostituisci il blocco logo:
```tsx
{/* PRIMA: */}
<div className="relative flex items-center justify-center w-8 h-8 rounded-lg bg-blue-600 shadow-lg shadow-blue-500/20 group-hover:scale-110 transition-all">
  <Zap className="w-4 h-4 text-white fill-white" />
</div>
<span className="text-lg font-black tracking-tighter text-white uppercase hidden sm:block">RepsBrief</span>

{/* DOPO: */}
<Image
  src="/logo.png"
  alt="RepsBrief"
  width={32}
  height={32}
  className="rounded-lg group-hover:scale-110 transition-all"
/>
<span className="text-lg font-black tracking-tighter text-white uppercase hidden sm:block">RepsBrief</span>
```

- [ ] **Step 2: Dashboard Sidebar — `src/components/layout/DashboardSidebar.tsx`**

Aggiungi import `Image` in cima:
```typescript
import Image from 'next/image'
```

Trova il logo nel sidebar (cerca `Zap className="w-4 h-4 text-white fill-white"` dentro il sidebar) e sostituisci il container:
```tsx
{/* PRIMA (logo nel sidebar, linea ~115): */}
<div className="relative flex items-center justify-center w-8 h-8 rounded-lg bg-blue-600 shadow-lg shadow-blue-500/20">
  <Zap className="w-4 h-4 text-white fill-white" />
</div>

{/* DOPO: */}
<Image
  src="/logo.png"
  alt="RepsBrief"
  width={32}
  height={32}
  className="rounded-lg"
/>
```

Rimuovi `Zap` dall'import `lucide-react` se non è usato altrove nel file.

- [ ] **Step 3: Mobile Nav — `src/components/layout/MobileNav.tsx`**

Aggiungi import `Image` in cima:
```typescript
import Image from 'next/image'
```

Rimuovi `Zap` dall'import `lucide-react`.

Sostituisci il logo nella top bar mobile:
```tsx
{/* PRIMA: */}
<div className="relative flex items-center justify-center w-8 h-8 rounded-lg bg-gradient-to-br from-blue-500 to-blue-700 shadow-lg shadow-blue-500/20">
  <Zap className="w-4 h-4 text-white fill-white" />
</div>
<span className="text-lg font-bold tracking-tight text-white">RepsBrief</span>

{/* DOPO: */}
<Image
  src="/logo.png"
  alt="RepsBrief"
  width={32}
  height={32}
  className="rounded-lg"
/>
<span className="text-lg font-bold tracking-tight text-white">RepsBrief</span>
```

- [ ] **Step 4: Login page — `src/app/(auth)/login/page.tsx`**

Aggiungi import `Image` e rimuovi `Zap` dall'import `lucide-react`:
```typescript
import Image from 'next/image'
import { Loader2 } from "lucide-react"  // solo Loader2 rimane
```

Sostituisci il logo nell'header della card:
```tsx
{/* PRIMA: */}
<div className="flex justify-center mb-4">
  <Zap className="w-10 h-10 text-blue-500 fill-blue-500" />
</div>

{/* DOPO: */}
<div className="flex justify-center mb-4">
  <Image src="/logo.png" alt="RepsBrief" width={48} height={48} className="rounded-xl" />
</div>
```

- [ ] **Step 5: Signup page — `src/app/(auth)/signup/page.tsx`**

Aggiungi import `Image` e rimuovi `Zap` dall'import `lucide-react` (mantieni gli altri icon usati).

Cerca il logo nella signup page (cerca `Zap` nell'JSX) e sostituiscilo:
```tsx
{/* PRIMA (qualunque form abbia il logo Zap): */}
<Zap className="w-8 h-8 text-blue-500 fill-blue-500" />

{/* DOPO: */}
<Image src="/logo.png" alt="RepsBrief" width={40} height={40} className="rounded-xl" />
```

- [ ] **Step 6: Commit**

```bash
git add src/components/landing/Navbar.tsx src/components/layout/DashboardSidebar.tsx src/components/layout/MobileNav.tsx src/app/'(auth)'/login/page.tsx src/app/'(auth)'/signup/page.tsx
git commit -m "feat: replace Zap placeholder with real RepsBrief logo across app"
```

---

## Task 3: Fix copy pricing misleading

Due problemi di onestà nel copy che creano abbandono al checkout:
1. **PricingNexus Pro card** dice "No credit card required to start." — FALSO: Stripe richiede la carta anche per i trial.
2. **HeroSection** dice "Pro includes 7-day free trial · No credit card required" — contraddittorio.

**Files:**
- Modify: `src/components/landing/PricingNexus.tsx` (riga 73)
- Modify: `src/components/landing/HeroSection.tsx` (riga 97)

- [ ] **Step 1: Fix PricingNexus Pro card copy**

In `src/components/landing/PricingNexus.tsx`, riga 73, sostituisci:
```tsx
{/* PRIMA: */}
<p className="text-xs text-slate-500 mt-2 font-medium">No credit card required to start.</p>

{/* DOPO: */}
<p className="text-xs text-slate-500 mt-2 font-medium">Card required · Cancel anytime, no questions asked.</p>
```

- [ ] **Step 2: Fix HeroSection subtitle copy**

In `src/components/landing/HeroSection.tsx`, riga ~97, sostituisci:
```tsx
{/* PRIMA: */}
<p className="text-[11px] text-slate-600 font-medium">
  Free forever · <span className="text-emerald-500 font-bold">Pro includes 7-day free trial</span> · No credit card required
</p>

{/* DOPO: */}
<p className="text-[11px] text-slate-600 font-medium">
  Free plan forever · No credit card required · <span className="text-emerald-500 font-bold">Pro: 7-day trial, then $19/mo</span>
</p>
```

Questo rende chiaro che: il piano gratuito non richiede carta, il Pro trial sì (implicitamente, perché ha un prezzo).

- [ ] **Step 3: Commit**

```bash
git add src/components/landing/PricingNexus.tsx src/components/landing/HeroSection.tsx
git commit -m "fix: correct misleading 'no credit card required' copy on Pro plan pricing"
```

---

## Task 4: Social proof dinamica — dato reale da Supabase

Sostituire "2,400+ briefs generated" hardcoded con il conteggio reale dei brief nel DB.

**Approccio:** API route pubblica `/api/stats` → chiamata fetch in HeroSection (già `'use client'`) con `useEffect` + fallback al valore precedente se fetch fallisce.

**Files:**
- Create: `src/app/api/stats/route.ts`
- Modify: `src/components/landing/HeroSection.tsx`

- [ ] **Step 1: Crea l'API route `/api/stats/route.ts`**

```typescript
// src/app/api/stats/route.ts
import { NextResponse } from 'next/server'
import { getSupabaseAdmin } from '@/lib/supabase'

export const dynamic = 'force-dynamic'
export const revalidate = 0

export async function GET() {
  try {
    const supabase = getSupabaseAdmin('api/stats')
    const { count, error } = await supabase
      .from('briefs')
      .select('*', { count: 'exact', head: true })

    if (error) throw error

    return NextResponse.json({ briefCount: count ?? 0 }, {
      headers: { 'Cache-Control': 's-maxage=3600, stale-while-revalidate=86400' }
    })
  } catch {
    // On error, return a safe fallback — don't expose internals
    return NextResponse.json({ briefCount: 0 })
  }
}
```

- [ ] **Step 2: Aggiorna HeroSection per fetch del conteggio reale**

In `src/components/landing/HeroSection.tsx`:

Aggiungi `useEffect` all'import di React già presente:
```typescript
import { useState, useEffect } from 'react'
```

Aggiungi all'inizio del componente `HeroSection` (è la funzione esportata, non `EmailCaptureForm`):
```typescript
export function HeroSection() {
  const [briefCount, setBriefCount] = useState<number | null>(null)

  useEffect(() => {
    fetch('/api/stats')
      .then(r => r.json())
      .then(data => {
        if (typeof data.briefCount === 'number' && data.briefCount > 0) {
          setBriefCount(data.briefCount)
        }
      })
      .catch(() => {/* silently fail, show fallback */})
  }, [])
```

Sostituisci il JSX del social proof (cerca `2,400+`):
```tsx
{/* PRIMA: */}
<span>2,400+ briefs generated</span>

{/* DOPO: */}
<span>
  {briefCount !== null
    ? `${briefCount.toLocaleString()}+ briefs generated`
    : '2,400+ briefs generated'}
</span>
```

- [ ] **Step 3: Verifica che HeroSection esporti la funzione con la firma corretta**

La funzione `HeroSection` deve iniziare così (dopo la modifica):
```typescript
export function HeroSection() {
  const [briefCount, setBriefCount] = useState<number | null>(null)

  useEffect(() => { ... }, [])

  return (
    <section ...>
```

- [ ] **Step 4: Commit**

```bash
git add src/app/api/stats/route.ts src/components/landing/HeroSection.tsx
git commit -m "feat: replace hardcoded social proof with real brief count from Supabase"
```

---

## Checklist finale pre-lancio

Dopo i 4 task:

- [ ] `pnpm build` passa senza errori TypeScript
- [ ] Deploy su Vercel andato a buon fine
- [ ] Landing page: title tab mostra "RepsBrief — AI Content Briefs from Real Trends"
- [ ] OG preview su [opengraph.xyz](https://www.opengraph.xyz) mostra logo e titolo corretto
- [ ] Logo visibile su: landing navbar, dashboard sidebar, mobile nav, login, signup
- [ ] PricingNexus Pro: copy mostra "Card required · Cancel anytime"
- [ ] `/api/stats` risponde con `{ briefCount: N }`
- [ ] Social proof in hero mostra numero reale (o fallback se 0 brief ancora)
