'use client'

// LockedIdeasOverlay is now rendered inline inside BriefList
// to allow fake cards to fill grid slots directly after the last visible card.
// This file is kept for any external imports.

export function LockedIdeasOverlay() {
  return null
}

// Legacy export alias
export { LockedIdeasOverlay as LockedIdeasGate }
